var minhaVar = 'minha variavel';

function minhaFunc(x, y) {
    return x + y;
}

//ES6
let number = 2;
const PI = 3.16;

var numeros = [1,2,3];
numeros.map(function(valor) {
    return valor*2;
});
numeros.map(valor => valor * 2);

class matematica {
    soma(x,y){
        return x+y;
    }
}


var n1: string = 'rsrsrs';